name: Deploy to Development

on:
  push:
    branches:
      - lmangallon/icp-383-implement-serverless-rust-function-for-email-proxy-call

concurrency:
  group: juno-deploy-dev-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Test secrets availability
        run: |
          if [ -z "$JUNO_DEV_TOKEN" ]; then
            echo "❌ JUNO_DEV_TOKEN is not set"
            exit 1
          else
            echo "✅ JUNO_DEV_TOKEN is available (length: ${#JUNO_DEV_TOKEN})"
          fi

          if [ -z "$NOTIFICATIONS_TOKEN" ]; then
            echo "❌ NOTIFICATIONS_TOKEN is not set"
            exit 1
          else
            echo "✅ NOTIFICATIONS_TOKEN is available (length: ${#NOTIFICATIONS_TOKEN})"
          fi
        env:
          JUNO_SATELLITE_ID: 5yoof-ciaaa-aaaal-asevq-cai
          JUNO_DEV_TOKEN: ${{ secrets.JUNO_DEV_TOKEN }}
          NOTIFICATIONS_TOKEN: ${{ secrets.NOTIFICATIONS_TOKEN }}

      - name: Deploy to Development Satellite
        uses: junobuild/juno-action@v0.4.9
        with:
          args: deploy --mode staging
        env:
          JUNO_SATELLITE_ID: 5yoof-ciaaa-aaaal-asevq-cai
          JUNO_TOKEN: ${{ secrets.JUNO_DEV_TOKEN }}
          NOTIFICATIONS_TOKEN: ${{ secrets.NOTIFICATIONS_TOKEN }}
